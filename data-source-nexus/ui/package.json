{
  "name": "com.quantnexus.data-source-nexus-ui",
  "version": "1.0.0",
  "description": "QuantNexus Data Source Plugin - UI Module (Browser-compatible)",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "clean": "rm -rf dist",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.3.0"
  },
  "peerDependencies": {
    "@quantnexus/types": "workspace:*"
  }
}
