{
  "name": "com.quantnexus.data-source-nexus",
  "version": "1.2.0",
  "description": "QuantNexus Data Plugin - Market data provider with Bridge integration",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "lifecycle": {
    "onInstall": "./scripts/install.js",
    "onUpgrade": "./scripts/upgrade.js",
    "onUninstall": "./scripts/uninstall.js"
  },
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "clean": "rm -rf dist",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@clickhouse/client": "^1.0.0",
    "@quantnexus/bridge": "workspace:*",
    "better-sqlite3": "^11.0.0"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.11",
    "typescript": "^5.3.0"
  },
  "peerDependencies": {
    "@quantnexus/types": "workspace:*"
  }
}
