{
  "name": "com.quantnexus.data-source-nexus-backend",
  "version": "1.0.0",
  "description": "QuantNexus Data Source Plugin - Backend Module (Node.js)",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "clean": "rm -rf dist",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@clickhouse/client": "^1.0.0",
    "better-sqlite3": "^11.0.0"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.0",
    "electron": "^28.0.0",
    "typescript": "^5.3.0"
  }
}
