{
  "name": "@quantnexus/plugin-data",
  "version": "1.0.0",
  "description": "QuantNexus Data Plugin - Market data provider",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "clean": "rm -rf dist",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@clickhouse/client": "^1.0.0",
    "better-sqlite3": "^11.0.0"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.11",
    "typescript": "^5.3.0"
  },
  "peerDependencies": {
    "@quantnexus/types": "workspace:*"
  }
}
